<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>尴尬时刻生成器 - 恶搞版</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Comic Sans MS', '微软雅黑', sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
            overflow-x: hidden;
        }
        
        .container {
            max-width: 800px;
            width: 90%;
            text-align: center;
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            margin-top: 20px;
            position: relative;
            overflow: hidden;
        }
        
        h1 {
            color: #ff4081;
            font-size: 2.8rem;
            margin-bottom: 20px;
            text-shadow: 3px 3px 0 #ffeb3b;
            transform: rotate(-2deg);
            animation: bounce 2s infinite alternate;
        }
        
        .subtitle {
            color: #2196f3;
            font-size: 1.5rem;
            margin-bottom: 30px;
            transform: rotate(1deg);
        }
        
        .generator-box {
            background: linear-gradient(45deg, #e1f5fe, #fff9c4);
            border: 5px dashed #ff9800;
            border-radius: 15px;
            padding: 25px;
            margin: 25px 0;
            min-height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }
        
        #scenario-text {
            font-size: 1.8rem;
            color: #d32f2f;
            line-height: 1.5;
            text-shadow: 1px 1px 0 #fff;
        }
        
        .generate-btn {
            background: linear-gradient(45deg, #ff4081, #7b1fa2);
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 1.5rem;
            border-radius: 50px;
            cursor: pointer;
            margin: 20px 0;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        .generate-btn:hover {
            transform: scale(1.05) rotate(2deg);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
        }
        
        .generate-btn:active {
            transform: scale(0.95) rotate(-2deg);
        }
        
        .counter {
            font-size: 1.2rem;
            color: #795548;
            margin-top: 15px;
        }
        
        .emoji {
            font-size: 3rem;
            margin: 10px;
            display: inline-block;
            animation: float 3s infinite ease-in-out;
        }
        
        .emoji:nth-child(2n) {
            animation-delay: 0.5s;
        }
        
        .emoji:nth-child(3n) {
            animation-delay: 1s;
        }
        
        .hidden-message {
            margin-top: 30px;
            font-size: 0.9rem;
            color: #757575;
            opacity: 0.7;
        }
        
        .confetti {
            position: absolute;
            width: 10px;
            height: 10px;
            background-color: #f44336;
            opacity: 0;
        }
        
        @keyframes bounce {
            0% { transform: translateY(0) rotate(-2deg); }
            100% { transform: translateY(-10px) rotate(-2deg); }
        }
        
        @keyframes float {
            0% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
            100% { transform: translateY(0); }
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .spinning {
            animation: spin 2s linear infinite;
            display: inline-block;
        }
        
        /* 响应式设计 */
        @media (max-width: 600px) {
            h1 {
                font-size: 2rem;
            }
            
            .subtitle {
                font-size: 1.2rem;
            }
            
            #scenario-text {
                font-size: 1.4rem;
            }
            
            .generate-btn {
                padding: 12px 30px;
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>尴尬时刻生成器</h1>
        <p class="subtitle">点击按钮，生成让你脚趾抠地的尴尬场景！</p>
        
        <div class="emoji">😳</div>
        <div class="emoji">🤦‍♂️</div>
        <div class="emoji">💩</div>
        <div class="emoji">🤡</div>
        <div class="emoji">🙈</div>
        
        <div class="generator-box">
            <p id="scenario-text">点击下方按钮开始生成尴尬场景！</p>
        </div>
        
        <button id="generate-btn" class="generate-btn">生成尴尬时刻</button>
        
        <div class="counter">
            已生成 <span id="counter">0</span> 个尴尬场景
        </div>
        
        <p class="hidden-message">
            小提示：连续点击5次按钮有惊喜！
        </p>
    </div>

    <script>
        // 尴尬场景数组
        const scenarios = [
            "你热情地向走过来的陌生人挥手，结果发现对方是在跟你身后的人打招呼。",
            "在安静的电梯里，你的肚子突然发出巨响，所有人都转头看你。",
            "你精心准备的笑话讲完后，全场鸦雀无声，只有你一个人在笑。",
            "你叫错了同事的名字，而且已经这样叫了三个月。",
            "在公共厕所里，你发现隔间没有卫生纸了，而你是唯一的使用者。",
            "你向朋友抱怨某个人有多讨厌，结果发现那个人是朋友的亲戚。",
            "你发错消息，把私密内容发到了公司大群，而且无法撤回。",
            "你模仿老板的走路姿势，结果一转身发现老板就在你身后。",
            "在重要会议上，你的手机突然响起，铃声是你自己录的搞笑歌曲。",
            "你误把洗手液当酸奶喝了一口，还当着所有人的面。",
            "你精心打扮去约会，结果发现对方穿的是完全一样的衣服。",
            "你试图帅气地跳过水坑，结果摔了个四脚朝天。",
            "你对着镜子练习演讲，后来发现镜子是单向的，外面的人一直在看你。",
            "你偷偷挖鼻孔，结果发现正在视频会议的摄像头没关。",
            "你给朋友发语音吐槽另一个人，结果不小心发给了被吐槽的人。"
        ];
        
        // 获取DOM元素
        const scenarioText = document.getElementById('scenario-text');
        const generateBtn = document.getElementById('generate-btn');
        const counterElement = document.getElementById('counter');
        
        let counter = 0;
        let clickCount = 0;
        
        // 生成随机尴尬场景
        function generateScenario() {
            const randomIndex = Math.floor(Math.random() * scenarios.length);
            scenarioText.textContent = scenarios[randomIndex];
            
            // 更新计数器
            counter++;
            counterElement.textContent = counter;
            
            // 增加点击计数
            clickCount++;
            
            // 每点击5次触发彩蛋
            if (clickCount % 5 === 0) {
                triggerEasterEgg();
            }
            
            // 添加动画效果
            scenarioText.style.animation = 'none';
            setTimeout(() => {
                scenarioText.style.animation = 'bounce 0.5s ease';
            }, 10);
            
            // 创建五彩纸屑效果
            createConfetti();
        }
        
        // 创建五彩纸屑效果
        function createConfetti() {
            const colors = ['#f44336', '#e91e63', '#9c27b0', '#673ab7', '#3f51b5', '#2196f3', '#03a9f4', '#00bcd4', '#009688', '#4CAF50'];
            
            for (let i = 0; i < 50; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';
                confetti.style.left = Math.random() * 100 + 'vw';
                confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                confetti.style.opacity = '1';
                document.body.appendChild(confetti);
                
                // 动画
                const animation = confetti.animate([
                    { transform: 'translateY(0) rotate(0deg)', opacity: 1 },
                    { transform: `translateY(${window.innerHeight}px) rotate(${Math.random() * 720}deg)`, opacity: 0 }
                ], {
                    duration: 1000 + Math.random() * 2000,
                    easing: 'cubic-bezier(0.215, 0.61, 0.355, 1)'
                });
                
                // 动画结束后移除元素
                animation.onfinish = () => {
                    confetti.remove();
                };
            }
        }
        
        // 触发彩蛋
        function triggerEasterEgg() {
            // 改变标题
            const title = document.querySelector('h1');
            title.textContent = '超级无敌尴尬生成器!!!';
            title.style.color = '#ff0000';
            title.style.textShadow = '5px 5px 0 #ffff00';
            title.classList.add('spinning');
            
            // 改变按钮文本
            generateBtn.textContent = '继续生成更多尴尬！';
            
            // 添加更多表情
            const emojis = ['🔥', '💥', '🚀', '🌈', '🎉', '👑', '💎', '🌟'];
            emojis.forEach(emoji => {
                const emojiElement = document.createElement('div');
                emojiElement.className = 'emoji';
                emojiElement.textContent = emoji;
                document.querySelector('.container').insertBefore(emojiElement, document.querySelector('.generator-box'));
            });
            
            // 3秒后恢复
            setTimeout(() => {
                title.textContent = '尴尬时刻生成器';
                title.style.color = '#ff4081';
                title.style.textShadow = '3px 3px 0 #ffeb3b';
                title.classList.remove('spinning');
            }, 3000);
        }
        
        // 添加按钮点击事件
        generateBtn.addEventListener('click', generateScenario);
        
        // 初始加载时生成一个场景
        window.addEventListener('load', () => {
            setTimeout(generateScenario, 500);
        });
    </script>
</body>
</html><!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>尴尬时刻生成器 - 恶搞版</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Comic Sans MS', '微软雅黑', sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 20px;
            overflow-x: hidden;
        }
        
        .container {
            max-width: 800px;
            width: 90%;
            text-align: center;
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            margin-top: 20px;
            position: relative;
            overflow: hidden;
        }
        
        h1 {
            color: #ff4081;
            font-size: 2.8rem;
            margin-bottom: 20px;
            text-shadow: 3px 3px 0 #ffeb3b;
            transform: rotate(-2deg);
            animation: bounce 2s infinite alternate;
        }
        
        .subtitle {
            color: #2196f3;
            font-size: 1.5rem;
            margin-bottom: 30px;
            transform: rotate(1deg);
        }
        
        .generator-box {
            background: linear-gradient(45deg, #e1f5fe, #fff9c4);
            border: 5px dashed #ff9800;
            border-radius: 15px;
            padding: 25px;
            margin: 25px 0;
            min-height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }
        
        #scenario-text {
            font-size: 1.8rem;
            color: #d32f2f;
            line-height: 1.5;
            text-shadow: 1px 1px 0 #fff;
        }
        
        .generate-btn {
            background: linear-gradient(45deg, #ff4081, #7b1fa2);
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 1.5rem;
            border-radius: 50px;
            cursor: pointer;
            margin: 20px 0;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }
        
        .generate-btn:hover {
            transform: scale(1.05) rotate(2deg);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.4);
        }
        
        .generate-btn:active {
            transform: scale(0.95) rotate(-2deg);
        }
        
        .counter {
            font-size: 1.2rem;
            color: #795548;
            margin-top: 15px;
        }
        
        .emoji {
            font-size: 3rem;
            margin: 10px;
            display: inline-block;
            animation: float 3s infinite ease-in-out;
        }
        
        .emoji:nth-child(2n) {
            animation-delay: 0.5s;
        }
        
        .emoji:nth-child(3n) {
            animation-delay: 1s;
        }
        
        .hidden-message {
            margin-top: 30px;
            font-size: 0.9rem;
            color: #757575;
            opacity: 0.7;
        }
        
        .confetti {
            position: absolute;
            width: 10px;
            height: 10px;
            background-color: #f44336;
            opacity: 0;
        }
        
        @keyframes bounce {
            0% { transform: translateY(0) rotate(-2deg); }
            100% { transform: translateY(-10px) rotate(-2deg); }
        }
        
        @keyframes float {
            0% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
            100% { transform: translateY(0); }
        }
        
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        
        .spinning {
            animation: spin 2s linear infinite;
            display: inline-block;
        }
        
        /* 响应式设计 */
        @media (max-width: 600px) {
            h1 {
                font-size: 2rem;
            }
            
            .subtitle {
                font-size: 1.2rem;
            }
            
            #scenario-text {
                font-size: 1.4rem;
            }
            
            .generate-btn {
                padding: 12px 30px;
                font-size: 1.2rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>尴尬时刻生成器</h1>
        <p class="subtitle">点击按钮，生成让你脚趾抠地的尴尬场景！</p>
        
        <div class="emoji">😳</div>
        <div class="emoji">🤦‍♂️</div>
        <div class="emoji">💩</div>
        <div class="emoji">🤡</div>
        <div class="emoji">🙈</div>
        
        <div class="generator-box">
            <p id="scenario-text">点击下方按钮开始生成尴尬场景！</p>
        </div>
        
        <button id="generate-btn" class="generate-btn">生成尴尬时刻</button>
        
        <div class="counter">
            已生成 <span id="counter">0</span> 个尴尬场景
        </div>
        
        <p class="hidden-message">
            小提示：连续点击5次按钮有惊喜！
        </p>
    </div>

    <script>
        // 尴尬场景数组
        const scenarios = [
            "你热情地向走过来的陌生人挥手，结果发现对方是在跟你身后的人打招呼。",
            "在安静的电梯里，你的肚子突然发出巨响，所有人都转头看你。",
            "你精心准备的笑话讲完后，全场鸦雀无声，只有你一个人在笑。",
            "你叫错了同事的名字，而且已经这样叫了三个月。",
            "在公共厕所里，你发现隔间没有卫生纸了，而你是唯一的使用者。",
            "你向朋友抱怨某个人有多讨厌，结果发现那个人是朋友的亲戚。",
            "你发错消息，把私密内容发到了公司大群，而且无法撤回。",
            "你模仿老板的走路姿势，结果一转身发现老板就在你身后。",
            "在重要会议上，你的手机突然响起，铃声是你自己录的搞笑歌曲。",
            "你误把洗手液当酸奶喝了一口，还当着所有人的面。",
            "你精心打扮去约会，结果发现对方穿的是完全一样的衣服。",
            "你试图帅气地跳过水坑，结果摔了个四脚朝天。",
            "你对着镜子练习演讲，后来发现镜子是单向的，外面的人一直在看你。",
            "你偷偷挖鼻孔，结果发现正在视频会议的摄像头没关。",
            "你给朋友发语音吐槽另一个人，结果不小心发给了被吐槽的人。"
        ];
        
        // 获取DOM元素
        const scenarioText = document.getElementById('scenario-text');
        const generateBtn = document.getElementById('generate-btn');
        const counterElement = document.getElementById('counter');
        
        let counter = 0;
        let clickCount = 0;
        
        // 生成随机尴尬场景
        function generateScenario() {
            const randomIndex = Math.floor(Math.random() * scenarios.length);
            scenarioText.textContent = scenarios[randomIndex];
            
            // 更新计数器
            counter++;
            counterElement.textContent = counter;
            
            // 增加点击计数
            clickCount++;
            
            // 每点击5次触发彩蛋
            if (clickCount % 5 === 0) {
                triggerEasterEgg();
            }
            
            // 添加动画效果
            scenarioText.style.animation = 'none';
            setTimeout(() => {
                scenarioText.style.animation = 'bounce 0.5s ease';
            }, 10);
            
            // 创建五彩纸屑效果
            createConfetti();
        }
        
        // 创建五彩纸屑效果
        function createConfetti() {
            const colors = ['#f44336', '#e91e63', '#9c27b0', '#673ab7', '#3f51b5', '#2196f3', '#03a9f4', '#00bcd4', '#009688', '#4CAF50'];
            
            for (let i = 0; i < 50; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';
                confetti.style.left = Math.random() * 100 + 'vw';
                confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                confetti.style.opacity = '1';
                document.body.appendChild(confetti);
                
                // 动画
                const animation = confetti.animate([
                    { transform: 'translateY(0) rotate(0deg)', opacity: 1 },
                    { transform: `translateY(${window.innerHeight}px) rotate(${Math.random() * 720}deg)`, opacity: 0 }
                ], {
                    duration: 1000 + Math.random() * 2000,
                    easing: 'cubic-bezier(0.215, 0.61, 0.355, 1)'
                });
                
                // 动画结束后移除元素
                animation.onfinish = () => {
                    confetti.remove();
                };
            }
        }
        
        // 触发彩蛋
        function triggerEasterEgg() {
            // 改变标题
            const title = document.querySelector('h1');
            title.textContent = '超级无敌尴尬生成器!!!';
            title.style.color = '#ff0000';
            title.style.textShadow = '5px 5px 0 #ffff00';
            title.classList.add('spinning');
            
            // 改变按钮文本
            generateBtn.textContent = '继续生成更多尴尬！';
            
            // 添加更多表情
            const emojis = ['🔥', '💥', '🚀', '🌈', '🎉', '👑', '💎', '🌟'];
            emojis.forEach(emoji => {
                const emojiElement = document.createElement('div');
                emojiElement.className = 'emoji';
                emojiElement.textContent = emoji;
                document.querySelector('.container').insertBefore(emojiElement, document.querySelector('.generator-box'));
            });
            
            // 3秒后恢复
            setTimeout(() => {
                title.textContent = '尴尬时刻生成器';
                title.style.color = '#ff4081';
                title.style.textShadow = '3px 3px 0 #ffeb3b';
                title.classList.remove('spinning');
            }, 3000);
        }
        
        // 添加按钮点击事件
        generateBtn.addEventListener('click', generateScenario);
        
        // 初始加载时生成一个场景
        window.addEventListener('load', () => {
            setTimeout(generateScenario, 500);
        });
    </script>
</body>
</html>
